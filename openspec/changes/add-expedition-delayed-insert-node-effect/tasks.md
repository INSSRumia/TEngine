## 1. Effect 与 schema 设计

- [x] 1.1 为 Expedition Effect schema 新增延迟插入节点 Effect 配置
- [x] 1.2 为 schema 新增简单临时节点描述结构，仅支持 `event` 与 `combat`
- [x] 1.3 为新增字段补充注释，明确 `passed_node_count` 使用 `1` 表示“下一个节点插入”
- [x] 1.4 梳理本次 schema 变更对应的 `xlsx` 手工改表清单，并暂停等待用户修改表格

## 2. 运行态结构升级

- [x] 2.1 在 `ExpeditionRunState` 中新增 `LstPendingInsertNode` 或等价运行时结构
- [x] 2.2 为待插入条目补充稳定顺序字段、来源字段与调试信息字段
- [x] 2.3 为运行时增加创建待插入条目、递减剩余计数与清理已消费条目的辅助方法

## 3. Effect 执行与队列插入

- [x] 3.1 实现延迟插入节点 Effect 的运行时逻辑，使其能够向运行态登记新的待插入请求
- [x] 3.2 在节点结果结算阶段接入待插入条目的统一递减与到期判断
- [x] 3.3 实现多个条目同位到期时按登记顺序插入队首的规则
- [x] 3.4 保持到期插入节点优先于普通主线路由节点执行

## 4. 临时节点执行支持

- [x] 4.1 为运行时临时 `event` 节点补充最小解析与执行支持
- [x] 4.2 为运行时临时 `combat` 节点补充最小解析与执行支持
- [x] 4.3 让临时节点在执行完成后以隐式 `fixed_next` 语义返回原有待执行队列
- [x] 4.4 确保临时 `combat` 节点失败时仍按现有远征结束规则终止流程

## 5. 配置生成与协作边界

- [x] 5.1 在用户完成 `xlsx` 修改后，重新生成 Luban 代码并接入新增配置
- [x] 5.2 若生成结果与运行时代码不匹配，记录具体差异并继续修正代码，不擅自改表

## 6. 验证与回归

- [x] 6.1 验证 `passed_node_count = 1` 时，插入节点成为下一个执行节点
- [x] 6.2 验证 `passed_node_count = 2` 时，插入节点在再经过一个节点后执行
- [x] 6.3 验证多个节点同位到期时，后登记的节点先执行
- [x] 6.4 验证临时 `event` 与临时 `combat` 节点执行后都能回到原流程
- [x] 6.5 验证最小远征闭环、随机事件节点与现有动态插入能力未被破坏
- [x] 6.6 进行编译与基础流程验证，确认与当前远征系统兼容
