## 1. 模块与基础类型搭建

- [x] 1.1 在 HotFix 模块目录新增 `EntityModule`，实现托管集合、注册/反注册入口及生命周期调度主循环。
- [x] 1.2 新增 `Entity` 基类（继承 `MonoBehaviour`），定义 `Priority` 属性与自定义生命周期函数签名。
- [x] 1.3 在 `Entity` 的 Unity 生命周期中仅保留桥接逻辑（`Awake` 注册、`OnDestroy` 反注册），移除业务时序依赖入口。
- [x] 1.4 将 `EntityModule` 接入 `GameModule` 门面，提供统一访问点（如 `GameModule.Entity`）。

## 2. 生命周期调度与顺序控制

- [x] 2.1 在 `EntityModule` 中实现实体生命周期阶段调用：初始化、启动、Update、LateUpdate、Shutdown。
- [x] 2.2 实现按 `Priority` 升序调度，并保证同优先级按注册顺序稳定执行。
- [x] 2.3 增加实体有效性过滤规则（对象销毁、失效引用、非活跃状态处理）并确保调度安全。

## 3. 运行期安全与一致性保障

- [x] 3.1 实现遍历期间增删保护：新增进入待并入队列、移除进入待清理队列，在安全点合并。
- [x] 3.2 保证实体在生命周期内“仅注册一次、仅清理一次”，避免重复调用与脏状态。
- [x] 3.3 在模块关闭流程中实现统一 `Shutdown` 分发并清空内部容器。

## 4. 迁移与验证

- [x] 4.1 选择 1~2 组存在时序问题的实体脚本迁移到 `Entity` 自定义生命周期函数。
- [ ] 4.2 验证初始化顺序稳定性（跨帧、跨场景、动态创建/销毁场景）并记录结果。
- [ ] 4.3 验证运行期行为：遍历中注册/销毁、同优先级稳定顺序、模块退出清理。
- [x] 4.4 清理迁移样例中对 Unity `Start/Update` 顺序的隐式业务依赖，确保与新规范一致。
