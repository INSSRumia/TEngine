## 1. MarbleRuntimeData 黑板结构搭建

- [x] 1.1 为 `MarbleRuntimeData` 引入 `State`、`Config`、`Frame` 三个内部数据区块
- [x] 1.2 将当前平铺字段按语义迁移到对应区块，保持 `MarbleRuntimeData` 作为统一黑板根对象
- [x] 1.3 确保各区块初始化方式稳定，不引入额外的运行时空引用问题

## 2. 字段归类与控制器迁移

- [x] 2.1 将生命、护盾、经验、目标、存活状态等归类到 `State`
- [x] 2.2 将攻击、防御、质量、缩放、升级阈值等配置投影字段归类到 `Config`
- [x] 2.3 将 `PriorityValueManager` 等控制器对象与帧级中间数据迁移到 `Frame`

## 3. Marble Ability 访问路径调整

- [x] 3.1 更新依赖 `MarbleRuntimeData` 平铺字段的 Marble abilities，使其通过结构化黑板访问数据
- [x] 3.2 调整结算与目标相关能力的读写路径，确保状态数据和帧数据访问边界清晰
- [x] 3.3 清理迁移后不再需要的旧字段访问方式

## 4. 访问约定与同步边界收敛

- [x] 4.1 确保能力默认通过 `State` 和 `Frame` 写入运行结果与帧级控制结果
- [x] 4.2 将 `Config` 视为配置投影区域，避免把它继续当作任意状态写入区使用
- [x] 4.3 保持未来网络同步可优先围绕 `State` 区设计，而不强依赖 `Frame` 内对象同步

## 5. 验证与回归

- [x] 5.1 在 Unity 中刷新资源并重新生成 `csproj`
- [x] 5.2 编译验证 Marble 相关运行时代码和 abilities 改动可以通过
- [x] 5.3 回归检查 Marble 的基础战斗流程、目标选择和能力控制逻辑未被破坏
