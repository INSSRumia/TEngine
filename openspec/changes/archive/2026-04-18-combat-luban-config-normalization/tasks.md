## 1. 行为字段枚举化

- [x] 1.1 盘点 `equip.xml`、`marble.xml`、`projectile.xml` 中仍以 `int` 表示行为分支的字段
- [x] 1.2 优先将 `shoot_type` 等高频行为字段改为显式 `enum`
- [x] 1.3 同步更新对应工厂和能力代码，使代码消费枚举语义而不是 magic number

## 2. 数值字段命名与类型收敛

- [x] 2.1 梳理时间类字段，统一持续时间、冷却时间、生命周期等字段的数值类型约定
- [x] 2.2 修正明显不合理的时间字段类型，例如将生命周期时间统一为浮点时间语义
- [x] 2.3 检查速度、角速度、加速度、角度字段命名，收敛到稳定命名规则

## 3. 主能力配置与扩展能力配置边界整理

- [x] 3.1 明确 `Equipment` 中主能力字段与 `lst_ability` 扩展字段的边界
- [x] 3.2 明确 `Projectile` 中核心配置字段与扩展能力字段的边界
- [x] 3.3 明确 `Marble` 中可扩展能力字段与固定运行参数字段的边界

## 4. 结构表达规则替代注释表达规则

- [x] 4.1 识别当前主要依赖长注释表达语义的关键配置字段
- [x] 4.2 优先通过 `enum`、字段名和 bean 层级把关键规则结构化
- [x] 4.3 保留注释作为补充说明，但避免注释继续承载核心行为语义

## 5. 工厂映射与生成结果联调

- [x] 5.1 更新受影响的 factory/ability 映射代码，保证字段改动后仍能正确消费 Luban 配置
- [ ] 5.2 在 Unity 中刷新配置生成结果并重新生成 `csproj`
- [ ] 5.3 在 Unity 编译链中验证配置 schema 变更和对应战斗代码可正常通过编译与基础运行验证
